<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<!-- mybatis的主配置文件 -->
<configuration>

    <!-- 配置外部文件 -->
    <properties resource="druid.properties"></properties>
    <!-- 配置分页插件 -->

    <!-- 官网环境配置 -->
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <!-- 配置数据源（连接池） -->
            <dataSource type="com.qf.config.MyDruidDataSourceFactory">
                <property name="driverClass" value="${driver}"/>
                <property name="jdbcUrl" value="${url}"/>
                <property name="username" value="${username}"/>
                <property name="password" value="${password}"/>
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <mapper resource="com/qf/dao/StudentMapper.xml"></mapper>
        <mapper resource="com/qf/dao/SubjectMapper.xml"></mapper>
        <mapper resource="com/qf/dao/CarMapper.xml"></mapper>

    </mappers>
</configuration>